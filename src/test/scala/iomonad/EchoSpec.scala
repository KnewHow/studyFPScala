package test.fpscala.iomonad

import fpscala.iomonad.io.IO
import scala.io.StdIn
import org.scalatest.FlatSpec

class EchoSpec extends FlatSpec {
  "test echo function" should "succeed" in {
    // IO.echo.run
    succeed
  }
}
